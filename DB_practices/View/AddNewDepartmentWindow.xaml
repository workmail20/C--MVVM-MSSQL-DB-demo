<Window x:Class="DB_practices.View.AddNewDepartmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DB_practices.View"
        mc:Ignorable="d"
        Title="AddNewDepartmentWindow" Height="200" Width="300"
        Name="AddNewDepartmentWnd">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="200">
            <TextBlock Text="Name"></TextBlock>
            <TextBox Name="NameBlock" Text="{Binding DepartmentName}"></TextBox>
        </StackPanel>
        <Button Grid.Row="1" Content="Add department" 
                Command="{Binding AddNewDepartment}"
                CommandParameter="{Binding ElementName=AddNewDepartmentWnd}"></Button>
    </Grid>
</Window>
